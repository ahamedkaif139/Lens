android {
    namespace "com.example.my_lens"
    compileSdkVersion 34

    defaultConfig {
        applicationId "com.example.my_lens"
        minSdkVersion 21
        targetSdkVersion 34
        versionCode flutterVersionCode.toInteger()
        versionName flutterVersionName
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
        }
    }
}

flutter {
    source '../..'
}
